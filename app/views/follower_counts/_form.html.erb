<%= form_for(@follower_count) do |f| %>
  <% if @follower_count.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@follower_count.errors.count, "error") %> prohibited this follower_count from being saved:</h2>

      <ul>
      <% @follower_count.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :candidate_id %><br>
    <%= f.text_field :candidate_id %>
  </div>
  <div class="field">
    <%= f.label :twitter_follwers %><br>
    <%= f.number_field :twitter_follwers %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
